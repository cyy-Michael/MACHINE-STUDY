# 前端接入边界（预留）

后续使用 Vue 3 + Element Plus + ECharts。建议页面按以下职责拆分：

- `Home`：项目介绍和入口。
- `Dataset`：数据集列表、字段和缺失值统计。
- `Experiment`：任务、算法和参数配置。
- `Training`：训练状态和日志。
- `Result`：指标、混淆矩阵、预测曲线、聚类散点和算法对比。

页面只通过 API 获取数据，不在浏览器中复制算法实现。
